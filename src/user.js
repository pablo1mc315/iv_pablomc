/**
 * Implementación de clase User
 */

class User
{
    /**
     * Metodos
     * @param {String} email 
     * @param {Array} genres 
     * @param {Array} films 
     */
    constructor(email,genres,films)
    {
        this.email = email;
        this.genres = genres;
        this.films = films;
    }
}