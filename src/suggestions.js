/**
 * Implementación de clase Suggestions, implementa a logica de negocio.
 */

class Suggestions
{
    /**
     * Metodos
     * @param {Array} films
     * @param {Array} users
     */
    constructor(films,users)
    {
        this.films = films;
        this.users = users;
    }
}